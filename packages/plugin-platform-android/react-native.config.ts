import {
  projectConfig,
  dependencyConfig,
} from '@react-native-community/cli-config-android';

export default {
  platforms: {
    android: {
      projectConfig,
      dependencyConfig,
    },
  },
};
