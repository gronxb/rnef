import {
  dependencyConfig,
  projectConfig,
} from '@react-native-community/cli-config-android';

export default {
  platforms: {
    android: {
      projectConfig,
      dependencyConfig,
    },
  },
};
